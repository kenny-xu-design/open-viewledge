# Next Task

Complete the v1.5.0 release-candidate manual acceptance against the exact
`release/v1.5.0` working state: launch the rebuilt internal Windows ZIP on a
clean writable Windows directory, configure a new user's text-analysis API,
create and inspect a real Bilibili multi-part knowledge set, register and run a
three-level local folder collection, create/start a page Intake, exercise fast
and deep global search, and restart the service to verify task/collection
recovery. Record failures without changing knowledge identity, package Schema,
the stable ASR Worker, or GPU-to-CPU fallback. After all checks pass, request
separate authorization for the release commit and tag; do not push, publish the
private ZIP, create a public repository, or begin v1.5.1 implementation in this
task.
