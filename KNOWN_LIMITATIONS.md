# Known limitations

- 首次启动需要联网安装 Python 依赖，发行包不复制开发机虚拟环境。
- 云端转写和文字分析取决于用户自行配置的第三方服务、网络、额度与服务条款。
- 基础包不包含本地语音模型；本地 CPU 和 GPU 转写需要安装模型扩展包。
- 基础包不安装 NVIDIA CUDA 运行库。本地 GPU 能力取决于用户已有环境。
- 部分网站不允许内嵌播放，工作台会提供“在原网站打开”；已有字幕、摘要和知识包仍可使用。
- 平台反爬、登录、地区限制或媒体下架可能导致元数据、字幕或评论不可获取。
- Web 服务默认仅监听本机地址，默认端口被占用时需要先关闭旧实例。
- 当前发行验证覆盖 Windows x64 与 Python 3.12；其他系统可从源码运行，但不属于本 Windows ZIP 的保证范围。
